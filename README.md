# NumberGuessGame

This is a simple Java web application that generates a random number when accessed.

## How to Run

1. Compile using Maven:
2. Deploy the generated `war` file to Tomcat.
3. Access the app at `http://100.26.42.154:8080/NumberGuessGame`

